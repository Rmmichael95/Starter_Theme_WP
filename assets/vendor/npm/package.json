{
  "name": "underscores",
  "version": "1.0.0",
  "description": "Starter theme based on _s, extended with Bootstrap, ACF blocks, and Dart Sass.",
  "author": "Automattic Theme Team",
  "license": "GPL-2.0-or-later",
  "keywords": [
    "WordPress",
    "Theme"
  ],
  "homepage": "https://github.com/Automattic/_s#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Automattic/_s.git"
  },
  "bugs": {
    "url": "https://github.com/Automattic/_s/issues"
  },
  "dependencies": {
    "bootstrap": "^5.3.8"
  },
  "devDependencies": {
    "@wordpress/scripts": "^19.2.2",
    "dir-archiver": "^1.1.1",
    "rtlcss": "^3.5.0",
    "sass": "^1.77.8"
  },
  "scripts": {
    "build": "npm run build:css && npm run build:blocks && npm run build:rtl",
    "build:css": "sass sass/style.scss:style.min.css --style=compressed --no-source-map",
    "build:blocks": "sass acf-blocks/:acf-blocks/ --style=compressed --no-source-map --error-css && find acf-blocks/ -name '*.css' -exec sh -c 'mv \"$1\" \"${1%.css}.min.css\"' _ {} \\;",
    "build:rtl": "rtlcss style.min.css style-rtl.min.css",
    "watch": "sass --watch sass/style.scss:style.css acf-blocks:acf-blocks --style=expanded --source-map",
    "lint:scss": "wp-scripts lint-style 'sass/**/*.scss'",
    "lint:js": "wp-scripts lint-js 'js/*.js'",
    "bundle": "dir-archiver --src . --dest ../_s.zip --exclude .DS_Store .stylelintrc.json .eslintrc .git .gitattributes .github .gitignore README.md composer.json composer.lock node_modules vendor package-lock.json package.json .travis.yml phpcs.xml.dist sass style.css.map yarn.lock"
  }
}
